div(class="company" itemscope itemtype="http://schema.org/BlogPosting") 
  div.companies-slider
    |{% for image in page.images %}
    //- div(class='company-hero {{page.images.title}}' style="background-image: url({{image.url}})")
    img(class='company-hero {{page.images.title}}' src="{{image.url}}")
    h1.company-title {{page.title}}
    div(class='company-content' itemprop='articleBody') {{content}}
    |{% endfor %}